import type { UserProfile } from '../types';

export const initialUserProfile: UserProfile = {
  userId: 'user_me',
  preferredEmotions: [],
  avoidedTraits: [],
  likedBookIds: ['book_002', 'book_006'],
};
