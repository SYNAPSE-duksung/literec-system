이 프로젝트는 UI_DESIGN_SPEC.md를 따른다.
- 컴포넌트는 데이터를 직접 만들지 않는다. 항상 /hooks를 통해서만 받는다.
- 반환 타입은 UI_DESIGN_SPEC.md 섹션 5.1과 100% 동일하게 유지한다.
- Tailwind, 라우팅 라이브러리, 상태관리 라이브러리 쓰지 않는다.
- 새 화면/컴포넌트 작업 전에는 UI_DESIGN_SPEC.md의 해당 섹션을 먼저 확인한다.